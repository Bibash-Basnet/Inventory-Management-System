@* @if (TempData["ApiResponse"] != null)
{
    <div class="alert alert-warning">
        <strong>API Response:</strong> @TempData["ApiResponse"]
    </div>
}

@model Inventory.Models.DTO.Product.ProductUpdateDto
@{
    ViewData["Title"] = "Edit Product";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="mb-3">
                <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Back
                </a>
            </div>

            <div class="card">
                <div class="card-header bg-warning">
                    <h4 class="mb-0"><i class="bi bi-pencil"></i> Edit Product</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post" enctype="multipart/form-data" id="editForm">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Product Name <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" placeholder="Enter product name" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="3" placeholder="Enter description"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Price" class="form-label">Price <span class="text-danger">*</span></label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="Price" type="number" step="0.01" class="form-control" placeholder="0.00" />
                                </div>
                                <span asp-validation-for="Price" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="Quantity" class="form-label">Quantity <span class="text-danger">*</span></label>
                                <input asp-for="Quantity" type="number" class="form-control" placeholder="0" />
                                <span asp-validation-for="Quantity" class="text-danger"></span>
                            </div>
                        </div>

                        @if (Model.ExistingImages != null && Model.ExistingImages.Any())
                        {
                            <div class="mb-3">
                                <label class="form-label">Current Images (Click X to remove)</label>
                                <div class="d-flex flex-wrap gap-2" id="imageContainer">
                                    @foreach (var image in Model.ExistingImages)
                                    {
                                        <div class="position-relative image-item" data-image-id="@image.Id">
                                            <img src="@Url.Content("https://localhost:7105" + image.ImageUrl)"
                                                 class="img-thumbnail"
                                                 style="width: 150px; height: 150px; object-fit: cover;"
                                                 alt="Product Image">
                                            <button type="button"
                                                    class="btn btn-danger btn-sm position-absolute top-0 end-0 m-1 delete-img-btn"
                                                    onclick="markForDeletion(@image.Id)">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </div>
                                    }
                                </div>
                            </div>
                        }

                        <input type="hidden" name="RemoveImageIds" id="removeImageIds" value="" />

                        @if (Model.ExistingImages != null)
                        {
                            @for (int i = 0; i < Model.ExistingImages.Count; i++)
                            {
                                <input type="hidden" name="ExistingImages[@i].Id" value="@Model.ExistingImages[i].Id" />
                                <input type="hidden" name="ExistingImages[@i].ImageUrl" value="@Model.ExistingImages[i].ImageUrl" />
                            }
                        }

                        <div class="mb-3">
                            <label asp-for="NewImages" class="form-label">Add New Images</label>
                            <input asp-for="NewImages" type="file" class="form-control" accept="image/*" multiple />
                            <div class="form-text">Select new images to add</div>
                            <span asp-validation-for="NewImages" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-2 justify-content-end">
                            <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-secondary">Cancel</a>
                            <button type="submit" class="btn btn-warning">Update Product</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        let imagesToDelete = [];

        function markForDeletion(imageId) {
            const imageItem = document.querySelector(`.image-item[data-image-id="${imageId}"]`);

            if (imageItem.classList.contains('marked-delete')) {
                // Unmark
                imageItem.classList.remove('marked-delete');
                imagesToDelete = imagesToDelete.filter(id => id !== imageId);
            } else {
                // Mark for deletion
                imageItem.classList.add('marked-delete');
                imagesToDelete.push(imageId);
            }

            // Update hidden field
            document.getElementById('removeImageIds').value = imagesToDelete.join(',');
        }
    </script>
} *@

@model Inventory.Models.DTO.Product.ProductUpdateDto
@{
    ViewData["Title"] = "Edit Product";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="mb-3">
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Back to Products
                </a>
            </div>

            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show">
                    @TempData["Error"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            <div class="card">
                <div class="card-header bg-warning">
                    <h4 class="mb-0"><i class="bi bi-pencil"></i> Edit Product</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post" enctype="multipart/form-data" id="editForm">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Product Name <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" placeholder="Enter product name" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="3" placeholder="Enter description"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Price" class="form-label">Price <span class="text-danger">*</span></label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="Price" type="number" step="0.01" class="form-control" placeholder="0.00" />
                                </div>
                                <span asp-validation-for="Price" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="Quantity" class="form-label">Quantity <span class="text-danger">*</span></label>
                                <input asp-for="Quantity" type="number" class="form-control" placeholder="0" />
                                <span asp-validation-for="Quantity" class="text-danger"></span>
                            </div>
                        </div>

                        @if (Model.ExistingImages != null && Model.ExistingImages.Any())
                        {
                            <div class="mb-3">
                                <label class="form-label">Current Images</label>
                                <p class="form-text text-muted mb-2">
                                    <i class="bi bi-info-circle"></i> Click image to mark for deletion
                                </p>
                                <div class="d-flex flex-wrap gap-2" id="imageContainer">
                                    @foreach (var image in Model.ExistingImages)
                                    {
                                        <div class="position-relative image-item" data-image-id="@image.Id" onclick="markForDeletion(@image.Id)" style="cursor: pointer;">
                                            <img src="@Url.Content("https://localhost:7105" + image.ImageUrl)"
                                                 class="img-thumbnail"
                                                 style="width: 150px; height: 150px; object-fit: cover;"
                                                 alt="Product Image">
                                            <button type="button"
                                                    class="btn btn-danger btn-sm position-absolute top-0 end-0 m-1 delete-img-btn">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </div>
                                    }
                                </div>
                            </div>
                        }

                        <!-- Hidden fields for images to remove -->
                        <div id="removeImageIdsContainer"></div>

                        <div class="mb-3">
                            <label asp-for="NewImages" class="form-label">Add New Images</label>
                            <input asp-for="NewImages" type="file" class="form-control" accept="image/*" multiple id="newImagesInput" />
                            <div class="form-text">Select new images to add (JPG, PNG, GIF, WEBP - Max 5MB each)</div>
                            <span asp-validation-for="NewImages" class="text-danger"></span>
                            <div id="newImagesPreview" class="d-flex flex-wrap gap-2 mt-2"></div>
                        </div>

                        <div class="d-flex gap-2 justify-content-end">
                            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                            <button type="submit" class="btn btn-warning">
                                <i class="bi bi-check-circle"></i> Update Product
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        let imagesToDelete = [];

        function markForDeletion(imageId) {
            const imageItem = document.querySelector(`.image-item[data-image-id="${imageId}"]`);

            if (imageItem.classList.contains('marked-delete')) {
                imageItem.classList.remove('marked-delete');
                imagesToDelete = imagesToDelete.filter(id => id !== imageId);
            } else {
                imageItem.classList.add('marked-delete');
                if (!imagesToDelete.includes(imageId)) {
                    imagesToDelete.push(imageId);
                }
            }

            // Update hidden fields
            const container = document.getElementById('removeImageIdsContainer');
            container.innerHTML = '';
            imagesToDelete.forEach(id => {
                const input = document.createElement('input');
                input.type = 'hidden';
                input.name = 'RemoveImageIds';
                input.value = id;
                container.appendChild(input);
            });

            console.log('Images to delete:', imagesToDelete);
        }

        // Preview new images
        document.getElementById('newImagesInput').addEventListener('change', function(e) {
            const previewContainer = document.getElementById('newImagesPreview');
            previewContainer.innerHTML = '';

            const files = e.target.files;
            if (files.length > 0) {
                for (let i = 0; i < files.length; i++) {
                    const file = files[i];

                    if (file.size > 5 * 1024 * 1024) {
                        alert(`File ${file.name} is too large. Maximum size is 5MB.`);
                        continue;
                    }

                    const reader = new FileReader();
                    reader.onload = function(event) {
                        const div = document.createElement('div');
                        div.className = 'position-relative';
                        div.innerHTML = `
                            <img src="${event.target.result}"
                                 class="img-thumbnail"
                                 style="width: 150px; height: 150px; object-fit: cover;"
                                 alt="New Image">
                            <span class="badge bg-success position-absolute top-0 start-0 m-1">New</span>
                        `;
                        previewContainer.appendChild(div);
                    };
                    reader.readAsDataURL(file);
                }
            }
        });
    </script>
}
